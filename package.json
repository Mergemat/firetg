{
  "name": "firetg",
  "version": "0.1.11",
  "module": "index.ts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Mergemat/firetg.git"
  },
  "bin": {
    "firetg": "index.ts"
  },
  "files": [
    "index.ts",
    "src",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "release": "bun test && bun run typecheck && bun scripts/release.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/qrcode-terminal": "^0.12.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "teleproto": "^1.225.4"
  }
}
